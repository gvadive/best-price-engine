server:
  port: 8081

spring:
  application:
    name: ingestion-service
  datasource:
    url: jdbc:h2:mem:ingestion;MODE=MySQL
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
